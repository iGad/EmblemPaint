<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:EmblemPaint.View"
                    xmlns:emblemPaint="clr-namespace:EmblemPaint"
                    xmlns:viewModel="clr-namespace:EmblemPaint.ViewModel">
    <DataTemplate DataType="{x:Type viewModel:ScreensaverViewModel}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"></RowDefinition>
                <RowDefinition Height="2*"></RowDefinition>
                <RowDefinition Height="5*"></RowDefinition>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="1" >
                Lorem ipsum dolor.
                <LineBreak/>
                Conectetur adipisicing elit.
            </TextBlock>
            <!--<TextBlock Grid.Row="1" Margin="0,64,0,0">
                Conectetur adipisicing elit.
            </TextBlock>-->

                <Button Grid.Row="2" 
              
                Command="{Binding Path=NextCommand}">  <!--VerticalAlignment="Bottom" 
                Margin="0,0,0,80" Style="{StaticResource YellowButton}"
                Width="250" 
                Height="80"
                Background="White"-->
                    <TextBlock>
                Начать
                </TextBlock>
            </Button>
        </Grid>
    </DataTemplate>
</ResourceDictionary>