<Window x:Class="EmblemPaint.View.WindowView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EmblemPaint"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:kernel="clr-namespace:EmblemPaint.Kernel;assembly=EmblemPaint.Kernel"
        mc:Ignorable="d"
        WindowStyle="None"
        ResizeMode="NoResize"
        Title="WindowView" Height="900" Width="500">
    <DockPanel>
        <DockPanel DockPanel.Dock="Top">
            <Grid x:Name="HomeButtonGrid"  Width="{Binding RelativeSource={RelativeSource Mode=Self}, Path=ActualHeight}">
                <Button Style="{StaticResource HomeButton}" CommandParameter="{StaticResource False}"/>
            </Grid>
            <Grid Background="#FF979797">
                
            </Grid>
        </DockPanel>
        <ContentPresenter DockPanel.Dock="Top" Content="{Binding}"></ContentPresenter>
    </DockPanel>
</Window>
